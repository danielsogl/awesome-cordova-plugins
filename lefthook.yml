pre-commit:
  parallel: true
  jobs:
    - name: prettier
      run: npx prettier --write --ignore-unknown {staged_files}
      stage_fixed: true

pre-push:
  jobs:
    - name: test
      run: npm test
